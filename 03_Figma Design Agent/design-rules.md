# design-rules.md

## 1. Page01 · 首页


### 1.1 新增模块

#### 模块尺寸

- 模块宽度：750px
- 模块高度：自适应内容高度

#### 模块圆角

- 模块整体圆角：24px（圆角 / Radius M）
- 模块内卡片圆角：12px（圆角 / Radius S）—— 注意此处指卡片，非标签
- 标签圆角：4px

#### 模块底色

- 固定使用 `#FFFFFF`
- 模块底色禁止使用其他颜色

#### 模块内边距

- 均 24px

#### 模块外边距

- 左右外边距：0px
- 上下外边距：18px

#### 模块内元素间距

- 内容相对独立：24px
- 基础间距：18px 或 12px
- 同类型元素重复：6px

#### 文字规范

- 文字样式使用 `references/design-system/agent-tokens.json`
- 模块标题字号：30px（文字颜色 / 深色字 `color_darkgray`）
- 正文：28px（文字颜色 / 深色字 `color_darkgray`）
- 辅助文字：24px（文字颜色 / 深色字 `color_darkgray`）

#### 字体颜色

- 标题：`#0F131A`
- 文本：`#5C5F66`
- 辅助：`#919499`

#### 模块描边

- 模块级卡片不使用描边

#### 阴影

- 不使用阴影

---

## 2. Page02 · 列表页

Figma 原页面：[酒店 AI Design Context · Page02 列表页](https://www.figma.com/design/2CXckWUEm1b8tpa3v7SG1M/%E9%85%92%E5%BA%97-AI-Design-Context?node-id=158-10886&t=VVTWEd3djXVrJwCO-11)

### 2.1 新增模块：商品列表外

#### 模块圆角

- 模块整体圆角：24px（圆角 / Radius M）
- 模块内卡片圆角：12px（圆角 / Radius S）—— 注意此处指卡片，非标签
- 标签圆角：4px

#### 模块底色

根据新增模块所属类型，选用对应颜色：

| 类型 | 纯色 | 渐变 |
|------|------|------|
| 正常 | `#F2F3F5` | — |
| 会员 | `#FEF5EC` | `linear-gradient(92deg, #FEF5EC 0%, #FDEDDB 100%)` |
| 大促 | `#FFF0F0` | `linear-gradient(92deg, #FFECEC 0%, color_warning_5 100%)` |
| AI 相关 | `#F5F5FF` | — |

#### 内边距

- 均 24px

#### 模块内元素间距

- 内容相对独立：24px
- 基础间距：18px 或 12px
- 同类型元素重复：6px

#### 文字规范

- 模块标题字号：30px（文字颜色 / 深色字 `color_darkgray`）
- 正文：28px（文字颜色 / 深色字 `color_darkgray`）
- 辅助文字：24px（文字颜色 / 深色字 `color_darkgray`）

#### 字体颜色

- 标题：`#0F131A`
- 文本：`#5C5F66`
- 辅助：`#919499`

#### 模块描边

- 模块级卡片不使用描边

#### 阴影

- 不使用阴影

### 2.2 新增模块：商品列表内

#### 模块尺寸

- 宽度：750px

#### 模块圆角

- 模块整体圆角：0px（圆角 / Radius M）
- 模块内卡片圆角：12px（圆角 / Radius S）—— 注意此处指卡片，非标签
- 标签圆角：4px

#### 模块底色

根据新增模块所属类型，选用对应颜色：

| 类型 | 纯色 | 渐变 |
|------|------|------|
| 正常 | `#F2F3F5` | — |
| 会员 | `#FEF5EC` | `linear-gradient(92deg, #FEF5EC 0%, #FDEDDB 100%)` |
| 大促 | `#FFF0F0` | `linear-gradient(92deg, #FFECEC 0%, color_warning_5 100%)` |
| AI 相关 | `#F5F5FF` | — |

#### 内边距

- 均 24px

#### 外边距

- 左右：0px
- 上下：0px

#### 模块内元素间距

- 内容相对独立：24px
- 基础间距：18px 或 12px
- 同类型元素重复：6px

#### 文字规范

- 模块标题字号：30px（文字颜色 / 深色字 `color_darkgray`）
- 正文：28px（文字颜色 / 深色字 `color_darkgray`）
- 辅助文字：24px（文字颜色 / 深色字 `color_darkgray`）

#### 字体颜色

- 标题：`#0F131A`
- 文本：`#5C5F66`
- 辅助：`#919499`

#### 模块描边

- 模块级卡片不使用描边

#### 阴影

- 不使用阴影

## 3. Page03 · 详情页

Figma 原页面：[酒店 AI Design Context · Page03 详情页](https://www.figma.com/design/2CXckWUEm1b8tpa3v7SG1M/%E9%85%92%E5%BA%97-AI-Design-Context?node-id=226-19077&t=Uh3gZH2TauFdhzzi-11)

### 3.1 新增模块

#### 模块尺寸

- 模块宽度：750px
- 模块高度：自适应内容高度

#### 模块圆角

- 模块整体圆角：24px（圆角 / Radius M）
- 模块内卡片圆角：12px（圆角 / Radius S）—— 注意此处指卡片，非标签
- 标签圆角：4px

#### 模块底色

- 固定使用 `#FFFFFF`
- 模块底色禁止使用其他颜色

#### 模块内边距

- 均 24px

#### 模块外边距

- 左右外边距：0px
- 上下外边距：18px

#### 模块内元素间距

- 内容相对独立：24px
- 基础间距：18px 或 12px
- 同类型元素重复：6px

#### 文字规范

- 文字样式使用 `references/design-system/agent-tokens.json`
- 模块标题字号：36px（文字颜色 / 深色字 `color_darkgray`）
- 正文：28px（文字颜色 / 深色字 `color_darkgray`）
- 辅助文字：24px（文字颜色 / 深色字 `color_darkgray`）

#### 字体颜色

- 标题：`#0F131A`
- 文本：`#5C5F66`
- 辅助：`#919499`

#### 模块描边

- 模块级卡片不使用描边

#### 阴影

- 不使用阴影

## 4. Page04 · 下单页

Figma 原页面：[酒店 AI Design Context · Page04 下单页](https://www.figma.com/design/2CXckWUEm1b8tpa3v7SG1M/%E9%85%92%E5%BA%97-AI-Design-Context?node-id=230-4336&t=Uh3gZH2TauFdhzzi-11)

### 4.1 新增模块

#### 模块尺寸

- 模块宽度：714px
- 模块高度：自适应内容高度

#### 模块圆角

- 模块整体圆角：12px（圆角 / Radius S）—— 注意此处指模块，非标签
- 标签圆角：4px

#### 模块底色

- 固定使用 `#FFFFFF`
- 模块底色禁止使用其他颜色

#### 模块内边距

- 均 24px

#### 模块外边距

- 左右外边距：18px
- 上下外边距：18px

#### 模块内元素间距

- 内容相对独立：24px
- 基础间距：18px 或 12px
- 同类型元素重复：6px

#### 文字规范

- 文字样式使用 `references/design-system/agent-tokens.json`
- 模块标题字号：30px（文字颜色 / 深色字 `color_darkgray`）
- 正文：28px（文字颜色 / 深色字 `color_darkgray`）
- 辅助文字：24px（文字颜色 / 深色字 `color_darkgray`）

#### 字体颜色

- 标题：`#0F131A`
- 文本：`#5C5F66`
- 辅助：`#919499`

#### 模块描边

- 模块级卡片不使用描边

#### 阴影

- 不使用阴影

#### 模块固定布局

- 标题文字位于模块左上角，标题距模块左 / 上均 24px

## 5. 原子级设计规范

### 5.1 字体

- 默认中文：`PingFang SC`，备用 `SimHei` 或系统中文无衬线。
- 数字 / 货币：优先使用飞猪数字字体语义；若当前 Figma 文件无对应字体，使用项目已有数字样式或最接近样式。
- 品牌汉字：用于品牌向标题、活动氛围字、Banner 主标题等；需要体现倾斜、较强识别感和中等字重。

### 5.2 字号阶梯

| 层级 | 字号 | 字重 | 行高 |
|------|------|------|------|
| H1 | 48px | 500 | 140% |
| H2 | 42px | 500 | 140% |
| H3 | 36px | 500 | 140% |
| H4 | 30px | 500 | 140% |
| Paragraph | 30px | 400 | 170% |
| Body 1 | 28px | 400 / 500 | 140% |
| Body 2 | 26px | 400 / 500 | 140% |
| Body 3 | 24px | 400 / 500 | 140% |
| Body 4 | 22px | 400 / 500 | 140% |
| Caption | 20px | 400 / 500 | 140% |
| Caption 1 | 18px | 400 | 140% |

### 5.3 字重规则

- 标题、选中项、按钮、强调信息使用 `500`。
- 正文、说明、未选中项使用 `400`。
- 价格数字可用 `600 / 700`，并允许视觉底对齐和更紧凑行高。
- 同一模块内不要混用过多字号；优先用字重、颜色和间距建立层级。

## 6. 色彩

### 6.1 核心语义色

| 语义 | 推荐值 | 用途 |
|------|------|------|
| 深色字 / 深色图标 | `#0F131A` | 标题、正文、强层级文字 |
| 次级文字 | `#5C5F66` | 副标题、标签、次按钮文字 |
| 弱说明 | `#919499` | 弱说明、副文案、明细 |
| 禁用 / 弱边框 | `#D2D4D9` | 失效文案、禁用边框 |
| 页面灰底 | `#F2F3F5` | 页面底层背景、分割线、次级底 |
| 浅灰辅底 | `#F7F8FA` | 浅控件底、卡片内色块 |
| 白色卡片 | `#FFFFFF` | 卡片主背景、面板背景 |
| 品牌黄 | `#FFE033` | 主按钮、选中态、核心强调 |
| 品牌浅黄 | `#FFFCEB` | 轻选中底 |
| 品牌辅助黄 | `#FFF7CC` | 复选选中、区间选中、浅品牌底 |
| 靛蓝 | `#6666FF` | 链接、焦点、次级选中 |
| 靛蓝浅底 | `#EBEBFF` | 靛蓝浅标签、时间轴连线 |
| 警告红 | `#FF3333` | 删除、警告 |
| 支付橙红 | `#FF5533` | 价格、支付、优惠 |
| 次级转化橙 | `#FF8C1A` | 加入购物车等次级转化按钮 |
| 大促红 | `#FF0036` | 大促角标、小面积营销标识 |
| 大促紫 | `#9537FD` | 预售角标、小面积营销标识 |
| 正向绿 | `#149AA8` | 正向反馈、提示 |
| 会员浅底 | `#FEF5EC` | 会员 / 保障浅底 |
| 会员深字 | `#5D3521` | 会员 / 保障深字 |

### 6.2 使用规则

- 品牌黄用于核心选择和主操作，不作为大面积页面背景。
- `#FF5533` 用于价格和支付强转化，避免与警告红混用。
- 大促红 / 大促紫只用于角标、小标签、局部活动识别，禁止整屏铺色。
- 页面灰底用于底层；卡片底优先白色；卡片内部弱分区用浅灰辅底。
- 文案可读性优先，摄影图或渐变上的文字必须保证对比度。

## 7. 圆角

| Token / 规则 | 值 | 用途 |
|------|------|------|
| Radius L | 24px | 对话框、浮层、通栏卡片、大模块 |
| Radius M | 12px | 非通栏卡片、常规内容卡 |
| Radius S | 6px | 小标签、小控件 |
| 标签小圆角 | 4px | 特定小标签、活动角标 |
| 胶囊 | 高度的 1/2 | 主按钮、胶囊筛选、浮动操作 |

页面级规范可覆盖产品级圆角。例如酒店下单页模块整体常用 12px，列表和详情新增模块常用 24px。

## 8. 栅格、间距与版心

- 基础单位：6px。
- 常规页面水平边距：18px。
- 首页或重点内容流可使用 30px 水平边距，以页面级规范为准。
- 模块内边距常用：18px、24px。
- 模块内元素间距常用：6px、12px、18px、24px。
- 模块之间垂直间距常用：18px、24px、30px。
- 主内容应使用纵向 auto layout 或清晰分组；模块高度由内容驱动。
- 固定头部、底栏、浮层与内容区需要有明确避让关系，不遮挡主要信息。

## 9. 阴影与层级

- 默认少用阴影，优先用背景色差、分割线、留白和圆角表达层级。
- 确需强调浮层时，使用低透明、低扩散的单层阴影，同类组件保持一致。
- 禁止大模糊、深黑、多层堆叠的通用 AI 阴影。
- 文字叠在图片或渐变上时，可用克制阴影增强可读性，但不要出现厚描边效果。
